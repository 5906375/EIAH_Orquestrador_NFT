def upload_to_ipfs(metadata: dict) -> dict:
    # Código de upload real (usando ipfs-http-client ou infura)
    return {
        "tokenURI": "ipfs://abc123...",
        "ipfsHash": "abc123..."
    }
